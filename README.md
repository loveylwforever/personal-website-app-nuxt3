# 某某软件

某某软件的官网。首页、下载、定价、体验、项目和某某圈都在这里。

技术栈：Nuxt 4、Vue 3、Pinia、Sass。主题用 `@nuxtjs/color-mode`。

## 本地运行

需要 Node 18+ 和 pnpm 9。

```bash
pnpm install
pnpm dev
```

浏览器打开 [http://localhost:3000](http://localhost:3000)。

```bash
pnpm build    # 生产构建
pnpm preview  # 预览构建结果
```
